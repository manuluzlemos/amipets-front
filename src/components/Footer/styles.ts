import styled from "styled-components";

export const Container = styled.footer`
    background-color: var(--gray);
    padding: 1rem;
    
    text-align: center;
    color: #fff;
    font-size: 1rem;

    position: absolute;
	bottom: 0;
	width: 100%;
`;

